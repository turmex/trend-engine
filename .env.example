# FormCoach Trend Engine — Environment Variables
# Copy to .env and fill in your values

# ── Email (required) ──
# Gmail App Password: myaccount.google.com/apppasswords
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=xxxx-xxxx-xxxx-xxxx
RECIPIENT_EMAIL=bart@example.com
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# ── Reddit API (recommended) ──
# Create app: reddit.com/prefs/apps → "script" type
REDDIT_CLIENT_ID=your_client_id
REDDIT_CLIENT_SECRET=your_client_secret
REDDIT_USER_AGENT=FormCoachBot/1.0

# ── Claude API (recommended, powers the AI content strategy) ──
# Without this, the script falls back to template-based strategy
ANTHROPIC_API_KEY=sk-ant-xxxxx

# ── V2 additions (not needed yet) ──
# TWITTER_BEARER_TOKEN=
# SERPAPI_KEY=
